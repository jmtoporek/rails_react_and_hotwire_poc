
  <div class="table-responsive">

    <h1>React on Rails Demo</h1>

    <!--
    <table class="table table-striped table-sm">
      <thead>
        <tr>
          <th>Name</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @players.each do |player| %>
          <tr>
            <td><%= player.name %></td>
            <td><%= link_to 'Show', react_on_rails_player_path(player) %></td>
            <td><%= link_to 'Edit', edit_react_on_rails_player_path(player) %></td>
            <td><%= link_to 'Destroy', react_on_rails_player_path(player), method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    -->

    <%= react_component("PlayersSearchComponent", props: {
      players: @players
    }, prerender: false) %>

    <%= link_to 'New Player', new_react_on_rails_player_path, class: 'btn btn-primary' %>

  </div>
